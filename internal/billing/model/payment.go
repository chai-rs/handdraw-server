package model

const (
	// OrderIDPrefix identifies immutable successful payment history.
	OrderIDPrefix = "ord"
	// RefundIDPrefix identifies immutable refund history linked to its order.
	RefundIDPrefix = "rfnd"
)
